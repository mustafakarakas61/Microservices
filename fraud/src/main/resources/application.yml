server:
  port: 8081
spring:
  application:
    name: fraud
  datasource:
    password: 180200
    url: jdbc:postgresql://localhost:5432/fraud
    username: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true